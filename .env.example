# Cloudflare Web Analytics site token.
# Get it from dash.cloudflare.com → Analytics & logs → Web Analytics → site → JS snippet.
# This token is public (it ships in every page's HTML), so it is safe to commit.
# Leave unset to disable analytics in local builds.
NUXT_PUBLIC_CF_ANALYTICS_TOKEN=
